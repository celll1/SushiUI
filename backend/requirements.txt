# FastAPI & Web Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0

# Stable Diffusion & AI
torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.25.0  # Note: 0.36.0 has torchao bug, use main branch or 0.37.0+ when released
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.1
omegaconf>=2.3.0
peft>=0.7.0  # Required for LoRA support
sentencepiece>=0.2.0  # Required for TIPO tokenizer
protobuf>=4.21.0  # Required for TIPO model support
tipo-kgen>=0.1.0  # TIPO prompt optimization library

# ACE-Step 1.5 audio/music generation
vector_quantize_pytorch>=1.30.1  # ResidualFSQ used by the ACE-Step DiT audio tokenizer
einx>=0.4.3  # tensor-op dependency of vector_quantize_pytorch
frozendict>=2.4.7  # dependency of vector_quantize_pytorch
soundfile>=0.12.1  # primary FLAC writer + aud2aud reference-audio loader (ACE-Step); libsndfile, no torchcodec needed
torchaudio>=2.1.0  # aud2aud reference-audio load/resample fallback (ACE-Step); note: 2.x .save(flac) needs torchcodec, so ffmpeg is the reliable FLAC fallback
loguru>=0.7.0  # logging dep of the vendored ACE-Step turbo DiT (DCW correction module, imported unconditionally)

# Image Processing
Pillow>=10.2.0
opencv-python>=4.9.0
imageio>=2.33.0
scipy>=1.11.0  # Required for Lanczos resampling in latent resize mode
matplotlib>=3.7.0  # Required for controlnet-aux preprocessors
controlnet-aux>=0.0.7  # ControlNet preprocessors (depth, openpose, etc.)
onnxruntime-gpu>=1.16.0  # ONNX inference with GPU support (includes CPU fallback)
spandrel>=0.4.2  # Super-resolution model loader for the Upscale panel

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1

# Authentication
python-jose[cryptography]>=3.5.0  # JWT token handling

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
aiofiles>=23.2.1

# Training
tensorboard>=2.15.0  # TensorBoard logging for training metrics
tqdm>=4.66.0  # Progress bars for training loops
bitsandbytes>=0.41.0  # 8-bit optimizers (AdamW8bit)
